<%= form_with model: quote, local: true, class: 'usa-form' do |form| %>
  <% if quote.errors.any? %>
    <%= render partial: 'layouts/errors', locals: {resource: quote} %>
  <% end %>

  <%= form.label :passage, 'My Rad Quote', class: 'usa-label' %>
  <%= form.text_area :passage, class: 'usa-textarea' %>

  <%= form.label :author_id, 'Choose an author', class: 'usa-label' %>
  <%= collection_select(
    :quote, 
    :author_id, 
    Author.order(:slug), 
    :id, 
    :name,
    { selected: author_id },
    { class: 'usa-select' }
  )%>
  <%= form.submit %>
<% end %>
